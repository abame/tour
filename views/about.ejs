<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VoyageVersus - <%- title %></title>
    <link rel="stylesheet" href="css/styles.css">
    <link rel="stylesheet" href="css/about.css">
    <link rel="icon" type="image/x-icon" href="images/favicon-black.png">
</head>
<body>
    <%- include('partials/header') %>
    <main>
        <section class="about">
            <h1>About us</h1>
            <%- aboutUs %>
            <h2>Our story</h2>
            <%- ourStory %>
            <h2>Why choose us?</h2>
            <%- whyUs %>
            <h2>Our promise to you</h2>
            <%- ourTeamPromise %>
            <% if(ourTeam) { %>
                <h2>Meet the Team</h2>
                <div class="team">
                    <% for(let i=0; i < ourTeam.length; i++) { %>
                        <div class="team-member">
                            <img src="https:<%= ourTeam[1].fields.image.fields.file.url %>" alt="<%= ourTeam[i].fields.name %>">
                            <h3><%= ourTeam[i].fields.name %></h3>
                            <p><%= ourTeam[i].fields.role %></p>
                        </div>
                    <% } %>
                </div>
            <% } %>
        </section>
    </main>
    <%- include('partials/footer') %>
</body>
</html>
